
[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "atlas-openaire-ner"
version = "0.1.0"
description = "Pipeline NER/Keyphrase per ATLAS con interrogazione OpenAIRE Graph API"
requires-python = ">=3.10"
dependencies = []
